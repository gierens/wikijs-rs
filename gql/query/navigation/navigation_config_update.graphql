mutation NavigationConfigUpdate (
  $mode: NavigationMode!
) { 
  navigation {
    updateConfig(mode: $mode) {
      responseResult {
        succeeded
        errorCode
        slug
        message
      }
    }
  }
}
