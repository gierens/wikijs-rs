mutation NavigationConfigUpdate (
  $mode: NavigationMode!
) { 
  navigation {
    updateConfig(config: $config) {
      responseResult {
        succeeded
        errorCode
        slug
        message
      }
    }
  }
}
