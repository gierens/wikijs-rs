mutation StorageTargetUpdate($targets: [StorageTarget]) {
  storage {
    updateTargets(targets: $targets) {
      responseResult {
        succeeded
        errorCode
        slug
        message
      }
    }
  }
}
