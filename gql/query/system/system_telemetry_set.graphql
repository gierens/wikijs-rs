mutation TelemetrySet($enabled: Boolean!) {
  system {
    setTelemetry(enabled: $enabled) {
      responseResult {
        succeeded
        errorCode
        slug
        message
      }
    }
  }
}
