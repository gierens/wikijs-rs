mutation SystemExport($entities: [String!]!, $path: String!) {
  system {
    export(entities: $entities, path: $path) {
      responseResult {
        succeeded
        errorCode
        slug
        message
      }
    }
  }
}
