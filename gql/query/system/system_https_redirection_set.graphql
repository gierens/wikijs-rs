mutation SystemHttpsRedirectionSet($enabled: Boolean!) {
  system {
    setHTTPSRedirection(enabled: $enabled) {
      responseResult {
        succeeded
        errorCode
        slug
        message
      }
    }
  }
}
